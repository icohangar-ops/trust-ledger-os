# Optional locally. Required on the public Vercel / Glama connector.
MCP_BEARER_TOKEN=

# Optional Streamable HTTP listen overrides (never invent a public hostname).
# MCP_HTTP_HOST=127.0.0.1
# MCP_HTTP_PORT=8787
# MCP_HTTP_PATH=/mcp
# MCP_HTTP_JSON_RESPONSE=1
# MCP_HTTP_CORS_ORIGINS=*
